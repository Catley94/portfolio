import React from 'react';
import ReactContactForm from 'react-mail-form';


const Contact = (props) => {
    return (
        <ReactContactForm to="s.catley@live.co.uk" />
    )
}

export default Contact;